# Environment Configuration
# Set to 'production' for production deployment
ENVIRONMENT=development

# Server Configuration
# Port for the application server (default: 3000 for development, 8000 for production)
SERVER_PORT=3000
# Number of Gunicorn worker processes (recommended: 2-4 x CPU cores)
# Leave empty for auto-calculation based on CPU count
GUNICORN_WORKERS=
# Bind address (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
BIND_ADDRESS=127.0.0.1

# Database Configuration
DB_ENGINE=sqlite
# For MySQL, uncomment and configure:
# DB_ENGINE=mysql
# DB_NAME=racker
# DB_USER=racker_user
# DB_PASSWORD=your_secure_password
# DB_HOST=localhost
# DB_PORT=3306

# WebAuthn/Passkey Configuration
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=Racker
WEBAUTHN_ORIGIN=http://localhost:3000

# Authentication Settings
# Set to 'false' to disable authentication requirement
REQUIRE_AUTH=false

# MCP Server Configuration
# Set to 'true' to enable the MCP server for Claude integration
MCP_ENABLED=false
MCP_PORT=3001

# Power and Cooling Calculation Constants
# These are standard engineering constants used for resource calculations
# WATTS_TO_BTU: Conversion factor from watts to BTU/hr
#   Standard value: 3.412 (1 watt = 3.412 BTU/hr)
#   Used to calculate heat output from electrical power consumption
WATTS_TO_BTU=3.412

# BTU_PER_TON: BTU/hr per ton of refrigeration
#   Standard value: 12000 (1 ton of cooling = 12,000 BTU/hr)
#   Used to calculate HVAC cooling requirements in tons from BTU/hr
BTU_PER_TON=12000

# Sentry Configuration
# Get your DSN from https://sentry.io/settings/your-org/projects/your-project/keys/
# Leave empty to disable Sentry
SENTRY_DSN=

# Environment name (e.g., development, staging, production)
SENTRY_ENVIRONMENT=development

# Performance monitoring - traces sample rate (0.0 to 1.0)
# 0.0 = no performance data, 1.0 = 100% of transactions
# Recommended: 0.1 for production (10% of transactions)
SENTRY_TRACES_SAMPLE_RATE=0.0

# Profiling sample rate (0.0 to 1.0)
# Requires SENTRY_TRACES_SAMPLE_RATE to be set
# Recommended: 0.1 for production (10% of profiles)
SENTRY_PROFILES_SAMPLE_RATE=0.0

# Logging integration
# Enable Sentry to capture log messages
SENTRY_ENABLE_LOGGING=true

# Minimum logging level to capture (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# This sets the breadcrumb level
SENTRY_LOGGING_LEVEL=ERROR

# Minimum logging level to create Sentry events (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# This sets when to actually send events to Sentry
SENTRY_LOGGING_EVENT_LEVEL=ERROR
